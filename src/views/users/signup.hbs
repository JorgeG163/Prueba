<!DOCTYPE html>
<html>
<head>
    <title>Formulario de Registro</title>
    <!-- Enlace a la hoja de estilos de Bootstrap -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        /* Estilos para el contenedor opaco */
        .opaque-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgb(255, 255, 255); /* Ajusta el tamaño de la imagen para cubrir todo el contenedor */
            z-index: 1; /* Asegura que el contenedor esté por encima de otros elementos */
        }

        /* Estilos para ajustar el tamaño del logo */
        .logo {
            max-width: 100px; /* Ajusta el valor según el tamaño deseado */
        }

        .form-container {
            position: relative;
            z-index: 2; /* Asegura que el formulario esté encima del contenedor opaco */
            margin-top: 100px; /* Ajusta este valor para posicionar el formulario adecuadamente */
        }

        h3 {
            font-size: 15px;
        }

    </style>
</head>
<body>
    <!-- Contenedor opaco para tapar el contenido no deseado -->
    <div class="opaque-container"></div>

    <div class="container form-container">
        <div class="row">
            <div class="col-md-4 mx-auto">
                <div class="card mt-4 text-center">
                    <div class="card-header">
                        <h1 class="h4">
                            Account Register
                        </h1>
                    </div>
                    <div class="card-body">
                        <img class="rounded-circle mx-auto d-block logo m-4" src="/public/img/usuario2.png" alt="Logo">
                        <form action="/users/signup" method="POST">
                            <div class="form-group">
                                <input type="text" class="form-control" name="name" placeholder="Name" value="{{name}}">
                            </div>
                            <div class="form-group">
                                <input type="email" class="form-control" name="email" placeholder="Email" value="{{email}}">
                            </div>
                            <div class="form-group">
                                <input type="password" class="form-control" name="password" placeholder="Password" value="{{password}}">
                            </div>
                            <div class="form-group">
                                <input type="password" class="form-control" name="confirm_password" placeholder="Confirm Password" value="{{confirm_password}}">
                            </div>
                            <div class="form-group">
                                <input type="claveregistro" class="form-control" name="claveregistro" placeholder="Clave para registro" value="{{claveregistro}}">
                            </div>
                            <button class="btn btn-primary btn-block">
                                Registrar
                            </button>
                            <ul class="nav nav-treeview">
                                <li class="nav-item">
                                    <a href="/users/signin" class="nav-link active">
                                        <i class="far fa-circle nav-icon"></i>
                                        <p>ya tengo cuenta</p>
                                    </a>
                                </li>
                            </ul>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- No olvides incluir la librería de JavaScript de Bootstrap al final del cuerpo del documento -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>


